package com.shop.module.product.repository;

import com.shop.module.product.entity.LogProductView;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LogProductViewRepository extends JpaRepository<LogProductView, Long> {
}
